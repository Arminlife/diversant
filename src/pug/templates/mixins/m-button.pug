mixin button(data)
	a.button(href=data.href || '#' class=data.mod)
		.button__title= data.text
		.button__icon
			+icon('arrow')

mixin btn_v2(data)
	a.btn_v2(href=data.href || '#' class=data.mod)
		.btn_v2__title= data.text
		.btn_v2__icon
			+icon(data.icon ? data.icon : 'arrow')